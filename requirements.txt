# Dependencias do LAPTOP, nao do cluster.
#
# Nao ha pyspark aqui, e isso e de proposito: o driver Spark roda dentro do
# spark-master (veja o Makefile), onde o Python e o 3.8 da imagem oficial e os
# executores falam a mesma versao. O que sobra para o laptop e baixar os
# arquivos da Camara e abrir o notebook.
#
# O que o cluster precisa esta em docker/spark.Dockerfile.
boto3==1.35.99
requests==2.32.3
PyYAML==6.0.2
pandas==2.2.3
matplotlib==3.10.0
jupyterlab==4.3.4

# so para o deck: `make conferir` mede a folga de cada slide num chromium
# headless. o chromium em si nao vem no pip:
#     .venv/bin/playwright install chromium
playwright==1.63.0
